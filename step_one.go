package main

func main() {
	/*
		for i := 0; i < 3; i++ {
			fmt.Println("I like Go!")
		}
		age, name := profile()
		fmt.Println(name, age)
		a := stepOneExtendedWork()
		fmt.Println(a)
		fmt.Println(squarer())
		fmt.Println(lastDigit())
		fmt.Println(preLastDigit())
		var h, m int = clockReader()
		fmt.Print("It is ", h, " hours ", m, " minutes.")
		some_func()
		if_example()
	*/
	sameDigits()
}
